# Kaggle downloads (large; keep local only)
data/kaggle/*.csv
data/kaggle/*.zip

# Regenerated by pipeline (merge adapters → raw_*, then output/*)
data/raw_*.csv
output/*
!output/.gitkeep

# Python
__pycache__/
*.py[cod]
.Python
.venv/
venv/
.env
